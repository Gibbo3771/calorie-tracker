<!DOCTYPE html>
<html>
    <head>
        <link href="/style.css" rel="stylesheet" type="text/css" />
        <title>Nom Nom Nom</title>
    </head>
    <body>
    <div class="banner">
        <div>
            <h1>Nom Nom Nom</h1>
        </div>
    </div>
    <nav class="navigation" role="full-horizontal">
        <ul class="nav-menu">
            <% if !request.fullpath.match?(/welcome|create/) %>
                <li>
                    <a class="<%= "navigation-current" if request.fullpath.match?(/track/)%>" href="/track">Track</a>
                </li>
                <li class="nav-dropdown-menu">
                    <a class="<%= "navigation-current" if request.fullpath.match?(/profile/)%>">Profile</a>
                    <ul class="nav-submenu">
                        <li><p><a class="<%= "navigation-current" if request.fullpath.match?(/profile/)%>" href="/profile/<%= session[:profile_id] %>/edit">Edit</a></p></li>
                        <li><p><a class="<%= "navigation-current" if request.fullpath.match?(/setting/)%>" href="#">Settings</a></p></li>
                    </ul>
                </li>
            <% end %>  
            </ul>
    </nav>
    <%= yield %>
    </body>
</html>